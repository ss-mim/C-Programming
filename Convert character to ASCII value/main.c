#include<stdio.h>

int main()
{
    char ch; // Declare a char variable to store the input character
    printf("Enter any character: ");
    scanf("%c",&ch);
    printf("The ASCII value is = %d\n", ch);
    return 0;
}